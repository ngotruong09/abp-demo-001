var abp = abp || {};

abp.modals.editModal = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("ProductService");
    };

    return {
        initModal: initModal
    };
};
